# Khởi tạo chuỗi bytes
result_bytes = b""

# Chuỗi bytes cần thêm vào chuỗi kết quả
my_bytes = b"Hello, World!"

# Sử dụng toán tử +=
result_bytes += my_bytes

# Hoặc có thể sử dụng result_bytes = result_bytes + my_bytes

# In ra chuỗi kết quả
print(result_bytes)
